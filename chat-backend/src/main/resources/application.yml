spring:
  kafka:
    consumer:
      bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}
      group-id: chat
    producer:
      bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}

  data:
    mongodb:
      uri: ${MONGO_DB_URI}
      auto-index-creation: true